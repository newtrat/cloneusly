# Test Fixtures

Shared test utilities for unit, integration, and end-to-end tests.

- `database.ts` — isolated PostgreSQL test database setup and cleanup
- `factories.ts` — user and point account factories
- `ledger-assertions.ts` — balance and journal verification helpers
- `users.ts` — seeded user identifiers for tests
